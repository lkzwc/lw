# 2026-06-02 工作日志

## 全页面统一 navigationStyle: custom

### 修复 discuss-detail 双导航栏
- discuss-detail 的 WXML 已有自定义导航栏代码，但 JSON 缺少 navigationStyle: custom
- 在 discuss-detail.json 中添加 `"navigationStyle": "custom"`

### 为10个普通页面统一添加自定义导航栏
所有页面统一使用蓝色渐变导航栏（activity-detail 除外，使用绿色渐变以匹配活动主题）

**修改的页面（每个页面改4个文件：json/wxml/wxss/js）：**
1. admin - 管理后台
2. admin/notice - 公告管理
3. admin/post - 帖子管理
4. admin/skill - 技能管理
5. my-likes - 我的点赞
6. my-posts - 我的发布
7. post-detail - 帖子详情
8. skill-detail - 技能详情
9. activity-detail - 活动详情（绿色渐变 #3DB94F → #52C41A）
10. timeline-detail - 详情

**每个页面的修改内容：**
- JSON：添加 `"navigationStyle": "custom"`，移除 navigationBarTitleText/BackgroundColor/TextStyle
- WXML：在根容器开头插入 custom-nav 导航栏 + 占位 view
- WXSS：添加自定义导航栏样式（custom-nav/nav-placeholder/nav-bar/nav-back/nav-back-icon/nav-title/nav-right）
- JS：添加 statusBarHeight: 20 数据、onLoad 中获取系统 statusBarHeight、末尾添加 onBackTap 方法

**未改的页面：**
- news、webview：纯 web-view 页面，不需要自定义导航栏

## 左滑删除：手写实现改为 WeUI mp-slideview

### 拼车页面 carpool 改造
- carpool.json：注册 `mp-slideview` 组件
- WXML：移除手写 swipe-item/swipe-content/swipe-delete 结构，改为 `<mp-slideview>` 包裹 route-card
- WXSS：移除 `.swipe-item/.swipe-content/.swipe-delete/.delete-text` 样式
- JS：
  - 移除手写触摸事件代码（onTouchStart/Move/End + swipeIndex/touchStartX/Y/touchCurrentX）
  - 移除 formatRoutes 中的 swipeX 字段
  - 简化 onRouteTap（移除 swipeIndex 判断）
  - 添加 `slideButtons: [{ text: '删除', type: 'warn' }]`
  - 用 `onSlideButtonTap` 替代 `onDeleteTap`（通过 e.currentTarget.dataset 获取 index/id）

### 议事厅页面 discuss 新增左滑删除
- discuss.json：注册 `mp-slideview` 组件
- WXML：用 `<mp-slideview>` 包裹 topic-card
- JS：
  - 添加 `slideButtons: [{ text: '删除', type: 'warn' }]`
  - 添加 `onSlideButtonTap` 事件处理（软删除 discussions 集合文档，status 改为 'deleted'）

## 管理后台 mp-slideview 左滑删除改造

### 权限逻辑修正
- 普通用户：通过「我的发布」「我的点赞」管理自己的数据（左滑删除）
- 管理员：通过「管理后台」控制所有数据（左滑删除）
- 列表页（carpool/discuss）：不提供删除功能，已回退 mp-slideview

### 公告管理页 (admin/notice) 改造
- notice.json：添加 `mp-slideview` 组件注册
- notice.wxml：移除 `item-actions`（编辑/删除按钮），改为 `<mp-slideview>` 包裹 list-item
- notice.js：
  - data 中添加 `slideButtons: [{ text: '删除', type: 'warn' }]`
  - `onDeleteNotice` → `onSlideButtonTap`，本地过滤删除项（不再重新 loadNotices）
- notice.wxss：移除 `.item-actions/.btn-link/.btn-danger/.item-main` 样式

### 技能管理页 (admin/skill) 完善
- skill.json：已有 `mp-slideview` 组件注册（之前已加）
- skill.wxml：已有 mp-slideview 结构（之前已加）
- skill.js：
  - data 中添加 `slideButtons: [{ text: '删除', type: 'warn' }]`
  - `onDeleteSkill` → `onSlideButtonTap`，删除后本地过滤 + 更新 homeCount
- skill.wxss：移除 `.item-actions/.btn-danger/.item-main` 旧样式

## UI 细节修复

### 1. 管理后台帖子页 emoji → iconfont
- post.wxml：`👍` → `icon-aixin`，`💬` → `icon-pinglun`
- post.wxss：重构 `.item-meta` 为 flex 布局 + 添加 `.meta-icon-font/.meta-num` 样式

### 2. 管理后台 mp-slideview 删除按钮展示不全
- 原因：`.list` 容器 `overflow: hidden` 截断了滑出的按钮
- 修复：admin/post、admin/skill、admin/notice 三个页面的 `.list` 均改为 `overflow: visible`

### 3. 帖子评论弹框区分一二级评论
- community.js `loadComments`：映射中添加 `isReply` 和 `replyToUserName` 字段
- community.wxml：二级评论 `.comment-item.reply` 缩进布局 + "回复 @用户名" 蓝色前缀
- community.wxss：
  - `.comment-item.reply`：左 padding 72rpx + 浅色背景 #F8FAFB
  - `.comment-user-avatar.reply-avatar`：48rpx（比一级 66rpx 小）
  - `.reply-to`：蓝色 #4A90D9 文字

## UI 问题修复（第二轮）

### 1. 评论弹窗评论多时不能滚动
- 原因：scroll-view 没有固定高度，flex:1 在小程序 scroll-view 中不生效
- 修复：`.comment-scroll-area` 改为 `height: calc(70vh - 200rpx)`

### 2. 二级评论缩进不显示
- 原因：`isReply` 字段在数据库中不存在，post-detail 页面回复评论存的是 `parentId` + `replyToName`
- 修复：community.js 中 `isReply: !!item.isReply` → `isReply: !!item.parentId`，`replyToUserName` 兼容取 `item.replyToName || item.replyToUserName`

### 3. 管理后台帖子图标不显示
- WXML 已用 iconfont（icon-aixin/icon-pinglun），样式已设，代码正确
- iconfont.wxss 全局引入，font-family 有 !important，不会被覆盖

### 4. 技能/帖子/公告左滑删除按钮高度不对
- 修复：三个管理页面（post/skill/notice）的 mp-slideview 添加 `ext-class="slideview-item"`
- WXSS 添加：
  - `.slideview-item` 承接 border-bottom（从 .list-item 移出）
  - `.slideview-item .weui-slideview__btn__wrp` flex 居中
  - `.slideview-item .weui-slideview__btn` height: 100% !important + flex 居中

## 修复「我的技能」页面空白问题

### 根本原因
1. **navTitle 未动态更新**：页面使用了自定义导航栏（navigationStyle: custom），标题由 wxml 变量 `{{navTitle}}` 渲染，但 onLoad 中用了 `wx.setNavigationBarTitle()` 设置标题（对自定义导航栏无效），没有 `setData({ navTitle })`，导致"我的技能"页面标题始终显示"我的发布"
2. **carpool 类型未处理**：mine.js 跳转我的拼车传了 `type=carpool`，但 my-posts.js 的 loadData/onShow/删除等逻辑只处理了 post 和 skill，carpool 无数据加载逻辑也无 wxml 模板

### 修复内容
- **my-posts.js**：
  - onLoad 中用 `setData({ type, navTitle: titleMap[type] })` 替代 `wx.setNavigationBarTitle()`
  - 添加 carpools 数组到 data
  - loadData/loadMore 支持三种类型（post/skill/carpool）
  - onShow 刷新逻辑统一化
  - onSlideButtonTap 支持 carpool 类型（软删除 carpools 集合）
  - onItemTap 支持 carpool（拨打电话）
- **my-posts.wxml**：添加 carpool 列表模板（路线卡片 + 空状态）
- **my-posts.wxss**：添加拼车卡片样式（carpool-card/carpool-route/route-dot 等）
- **api.js**：userApi 新增 `getMyCarpools(openid, page, pageSize)` 方法

## 修复卡片背景与页面底色混融

### 问题
- `.post-card` 和 `.carpool-card` 使用 `var(--bg-primary)` (#F4F7FB) 作为背景色，与页面底色 #F5F7FA 几乎一致，卡片看不出来
- skill-card 组件虽为白色背景但无边框，在浅色底上也缺乏层次感

### 修复
- **my-posts.wxss**：`.post-card` 和 `.carpool-card` 背景改为 `var(--bg-card)` (#FFFFFF)，添加 `border: 1rpx solid var(--border-color)` (#E8F0F8)
- **skill-card.wxss**：`.skill-card` 添加 `border: 1rpx solid #E8F0F8`

## 修复卡片间距 + 技能左滑删除按钮高度

### 问题
1. 我的发布/我的技能/我的点赞/我的捎一段页面，卡片之间无间距，紧贴在一起
2. 技能列表中 mp-slideview 删除按钮高度和 skill-card 不一致

### 修复
- **my-posts.wxss**：添加 `mp-slideview { margin-bottom: 24rpx; }` 统一卡片间距；添加 `.slideview-item .weui-slideview__btn__wrp` 和 `.slideview-item .weui-slideview__btn` 样式让删除按钮高度跟随卡片
- **my-posts.wxml**：技能列表的 mp-slideview 添加 `ext-class="slideview-item"`
- **my-likes.wxss**：添加 `mp-slideview { margin-bottom: 24rpx; }`；`.like-card` 背景从 `var(--bg-primary)` 改为 `var(--bg-card)` + 加边框（与 my-posts 统一）

## 更新 iconfont 字体包（修复 icon-tieziguanli 不显示）

### 问题
- admin.wxml 中配置了 `<text class="iconfont icon-tieziguanli">` 但图标不显示
- 第一次修复：用户下载的是 Symbol 格式，base64 放进 iconfont.wxss 后只有 @font-face 没有 .icon-xxx:before 类映射，所以样式不生效
- 正确做法：需要 Font class 格式的 iconfont.css 中的类映射，+ base64 内联字体

### 修复（全链路更新，第二次修复）
1. **iconfont.wxss**：用新包（font_5177376_q28033cm97m，Font class 格式）的 woff2/woff base64 替换 + 补上所有 57 个 `.icon-xxx:before` 类映射 + 语义别名
   - 补上了遗漏的 `icon-send`(\10207) 和 `icon-arrow-right`(\e604)
2. **assets/fonts/iconfont.woff2 + iconfont.ttf**：用新包文件替换旧的
3. **app.js 硬编码 base64 fallback**：同步更新为新 woff2 的 base64 数据

## iconfont 辅助 class 全页面统一（全局替换）

### 改动内容
1. **iconfont.wxss 修复**：
   - `.iconfont` 基础类移除 `color: #4A90D9` 和 `margin-bottom: 12rpx`（会覆盖辅助 class 颜色 + 造成位移）
   - 删除 `.icon-gonggao` 语义别名 `\e60f` 覆盖（unicode 不在字体中导致图标不显示），保留自动生成版本 `\e607`
2. **所有页面 WXML/WXSS 全局替换**（通过多个并行 agent 完成）：
   - 旧 class → 新全局 class 对照：
     - `action-icon-font` → `icon-secondary icon-lg`（评论/分享）/ `icon-liked icon-lg`（点赞）/ `icon-admin icon-lg`（管理后台）
     - `empty-icon-font` → `icon-placeholder icon-huge`
     - `publish-icon-font` → `icon-white icon-xxxl`（发布按钮）/ `icon-muted icon-xl`（上传图片）
     - `search-icon-font` → `icon-white-light icon-md`
     - `meta-icon-font` → `icon-liked icon-sm` / `icon-secondary icon-sm`
     - `time-icon-font` → `icon-secondary icon-lg`
     - `phone-icon-font` → `icon-primary icon-lg`
     - `like-icon` → 拆分为 `icon-liked`(点赞态) / `icon-secondary`(默认态)
     - `avatar-icon-font` → `icon-muted icon-xxxl`
     - `detail-placeholder-icon` → `icon-placeholder-card icon-huge`
     - `info-location-icon` → `icon-secondary icon-md`
     - `info-time-icon` → `icon-time-accent icon-md`
     - `share-icon` → `icon-secondary icon-lg`
     - `placeholder-icon` → `icon-placeholder icon-xxxl`
     - `unlike-icon` → 已内化为 `icon-secondary`
     - `iconfont-liked` → `icon-liked`
   - 保留不动的：`nav-back-icon`（导航栏返回按钮，非 iconfont）、`notice-icon-font`（间距辅助）、`activity-icon`（间距辅助）、`skill-placeholder-icon`（字号覆盖）、`home-placeholder-icon`（字号覆盖）、`menu-icon-wrap`（容器样式）、`add-icon`（image 样式）
3. **iconfont.wxss 语义别名新增**：`icon-sousuo`、`icon-gonggao`、`icon-tiezhi`、`icon-gongju`、`icon-qiche`、`icon-rili`、`icon-dianhua`、`icon-tianjia`、`icon-aixin`、`icon-wode`、`icon-guanli`、`icon-liulan`、`icon-fenxiang`、`icon-shanchu`、`icon-location`、`icon-send`、`icon-arrow-right`、`icon-dianzan`、`icon-dianzan1`
4. **全局辅助 class 体系**（iconfont.wxss 集中管理）：
   - 颜色类：`icon-primary`(#4A90D9) / `icon-secondary`(#999) / `icon-muted`(#CCC) / `icon-white`(#FFF) / `icon-white-light`(rgba) / `icon-search-bar`(rgba) / `icon-placeholder`(#CCC,opacity:0.5) / `icon-placeholder-card`(#B0C4DE) / `icon-liked`(#FF6B6B) / `icon-admin`(#FF9800) / `icon-time-accent`(#FAAD14) / `icon-people-accent`(#52C41A)
   - 字号类：`icon-xs`(24rpx) `icon-sm`(28rpx) `icon-md`(32rpx) `icon-lg`(40rpx) `icon-xl`(44rpx) `icon-xxl`(56rpx) `icon-xxxl`(80rpx) `icon-huge`(120rpx)
